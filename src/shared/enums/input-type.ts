export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  SWITCH = 'switch',
}

export enum NonStandardInputType {
  DATE_OF_BIRTH = 'dateOfBirth',
}
