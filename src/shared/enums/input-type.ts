export enum EInputType {
  TEXT = 'text',
  PASSWORD = 'password',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  SWITCH = 'switch',
}

export enum ENonStandardInputType {
  DATE_OF_BIRTH = 'dateOfBirth',
}
