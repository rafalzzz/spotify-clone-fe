export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
}

export enum NonStandardInputType {
  DATE_OF_BIRTH = 'dateOfBirth',
}
