export enum EInputType {
  TEXT = 'text',
  PASSWORD = 'password',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  SWITCH = 'switch',
}

export const enum ENonStandardInputType {
  DATE_OF_BIRTH = 'dateOfBirth',
}
