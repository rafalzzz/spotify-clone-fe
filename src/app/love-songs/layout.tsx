import { FC, PropsWithChildren } from 'react';

import '@/styles/globals.scss';
import '@/styles/properties.scss';

const LoveSongs: FC<PropsWithChildren> = ({ children }): JSX.Element => <>{children}</>;

export default LoveSongs;
