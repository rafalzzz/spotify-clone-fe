import { FC, PropsWithChildren } from 'react';

import '@/styles/globals.scss';
import '@/styles/properties.scss';

const LoveAlbums: FC<PropsWithChildren> = ({ children }) => <>{children}</>;

export default LoveAlbums;
